pour creer un environemment virtuel sur MAC: 
    > python3.12 -m venv venv
    > source venv/bin/activate

pour installer nos outils:
    >pip install --upgrade pip
    > pip install langchain langchain-community langchain-mistralai fastapi uvicorn pypdf pgvector psycopg2-binary python-dotenv
    > pip install python-multipart
    >pip install streamlit requests

on lance docker:
> docker compose up -d

on cree un fihier requirements.txt pour garantir que l'environnement de production est identique à l'environnement de développement.:
>pip freeze > requirements.txt

on lance l'API dans un terminal:
>uvicorn app_api:app --reload
et on ouvre:
> http://127.0.0.1:8000/docs

on lance streamlit dans un deuxieme terminal: 
>streamlit run app.py